# Environment / secrets
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Data (raw dumps are reproducible via ETL; never commit)
data/raw/
data/interim/
data/corp_codes/
*.zip
*.parquet

# Airflow runtime
airflow/logs/
airflow/plugins/
airflow/*.pid
airflow/*.cfg
airflow/*.db

# Docker volumes
docker/volumes/

# 로컬 모델 산출물 (ONNX/INT8, 수 GB — 스크립트로 재생성 가능)
models/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Notebooks checkpoints
.ipynb_checkpoints/
